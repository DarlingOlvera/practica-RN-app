/**
 * @format
 */

import {AppRegistry} from 'react-native';
import {name as appName} from './app.json';
import { ProductosApp } from './src/ProductosApp';

AppRegistry.registerComponent(appName, () => ProductosApp);
